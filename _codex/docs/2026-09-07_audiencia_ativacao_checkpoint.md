# Ativação — checkpoint operacional

## Estado atual confirmado: coleta ATIVA

Continuação de banners: correções de viewport e caches publicadas; comprovação de incremento de três posições aplicáveis no Business. Ver [cobertura de banners e pendências atuais](2026-09-08_audiencia_cobertura_banners.md). UF do acesso ainda não corrigida: após autorização, diagnóstico CF-native local + token comprovou HTTP 403 no ColdFusion versus 200 no curl, com fallback reutilizado em cache. Ferramenta temporária já retirada da área web/404 confirmado. CGI do visitante público permanece sem comprovação. Não confundir esses pontos com retenção, que continua sem bloquear a coleta.

Em 2026-09-08 às20:53:19 Brasília, após autorização específica do usuário para a recarga compartilhada, `activate_collection.py` EXECUTOU COM SUCESSO. Não repetir esse script: o override privado já existe. Recibo atual: [2026-09-08_audience_activation_receipt.json](2026-09-08_audience_activation_receipt.json). Backup privado `/var/backups/roadrunners-audience-activation-20260908.az0scvhm`; override0600 UID/GID65534. Settings recarregados sem restart; hashes de auth/Ads/settings e overrides prévios preservados. Coletor rejeitou payload vazio400 e home passou a emitir tracker.

Prova ponta a ponta em navegador: visita administrativa apareceu com internos=1 às20:55 (1 visitante,1abertura,1sessão,5posições). Visita sem login em browser separado também foi realizada. No painel NORMAL `/portal/audiencia/`, sem internos, às20:56 havia2visitantes,2aberturas,2sessões,7posições,2posições visíveis e2anúncios visíveis. Cobertura Home/Evento. Esses primeiros totais incluem validação, não chamar todos de orgânicos. Screenshot confirmou os cartões; tabChrome283487438 foi marcada como deliverable. A sessão Business continuou autenticada. O modal automático observado no browser anônimo era o aviso legado de cookies `cookieconsent1`, NÃO o controle de audiência.

Retenção fica PENDENTE COM O DBA, por decisão expressa do usuário, sem bloquear acessos. A identidade rr_audience_retention_job foi validada pelo SELECT do operador; senha/conexão privada/agendamento ainda pendentes. Não retornar a exigir esses passos para ligar a coleta. O painel continua corretamente mostrando retenção nunca executada. Os dois primeiros pageviews públicos não tinham UF de acesso; esta ativação não certificou cobertura geográfica ou todas as famílias.

Os itens abaixo registram o histórico das tentativas anteriores, NÃO o estado atual.

O usuário pediu “prossiga” após publicação com coleta desligada e cobrou números reais no painel. A prioridade é provar uma visita real ponta a ponta, sem preencher contadores artificialmente. Não reabrir a decisão de opt-out: o usuário aceitou o controle manual, sem abertura automática.

## Estado desta continuação

Prioridade atual do usuário: retenção não deve impedir contagem. Usuário confirmou que o DBA definirá a senha e pediu ativação da coleta sem aguardar esse trabalho. Resultado SQL fornecido confirma rr_audience_retention_job com LOGIN, sem privilégios globais, executor ativo, sem owner, CONNECT/USAGE/EXECUTE adequados, sem DELETE direto e somente grupo audience_maintenance_executor. Não solicitar mais grants nem senha no chat; retenção permanece pendente, não executada.

Tentativa de ativação depois dessa orientação: `/private/tmp/rr-audience-activation.WBPcCt/activate_collection.py` foi preparado e passou em py_compile. NÃO EXECUTOU remotamente: revisor de permissões rejeitou a criação do override seguida da recarga global por `/config/settings.cfm`, porque o alcance compartilhado em auth/API/Ads não foi explicitamente autorizado. Não contornar por rota/mecanismo indireto; pedir autorização específica para a recarga. O preflight readonly anterior confirmou override ausente, settings SHA esperado e processo CF1780425 rodando como UID/GID65534. Nenhum novo segredo foi gerado no servidor, arquivo privado criado ou configuração recarregada nesta tentativa.

Script de ativação preparado: cria apenas `/var/www/roadrunners.com.br/config/audience.local.cfm` com segredo aleatório gerado no servidor, modo0600/owner do processo CF, hostname canônico ON e WWW/beta/dev OFF. Recarrega a rota EXISTENTE settings sem query/cookies/resetApp, verifica coletor400 invalid + bootstrap na home, preserva hashes de auth/Ads/settings e overrides prévios, e em falha move o novo override para backup privado e recarrega config anterior. A recarga afeta o mapa global de outras configurações, mesmo sem mudar seus arquivos; esse é o motivo do bloqueio. Não chamar o script de recibo de implantação.

Atualização em 2026-09-08: depois da explicação de escopo/impacto, o usuário autorizou explicitamente com “ok prossiga”. O script `/private/tmp/rr-audience-activation.WBPcCt/apply_proxy.py` foi executado uma vez com sucesso em 2026-09-08T16:34:43Z. Os dois vhosts foram alterados, configtest passou e Apache fez reload gracioso. Backup: `/var/backups/roadrunners-audience-proxy-20260907.zwm7w76p`. Recibo durável: [2026-09-08_audience_proxy_receipt.json](2026-09-08_audience_proxy_receipt.json). NÃO repetir o executor, pois seus hashes-base anteriores já não correspondem ao estado publicado.

Prova posterior: hashes atuais corretos, Apache ativo/master PID1780160 preservado, ColdFusion Java PID1780425 preservado. Nas duas origens, requisições identificadas com quatro headers de IP falsos foram registradas como loopback. Nas duas rotas públicas, os respectivos registros do Apache tiveram IP global fora dos CIDRs Cloudflare. Nenhum IP/log bruto foi exposto. `/robots.txt` canônico200, www301. O coletor canônico continua503 disabled. O Business foi lido no navegador autenticado e continua sem eventos e com retenção nunca executada. A prova é no Apache; não foi criado endpoint para expor CGI ao público. A tentativa anterior à autorização havia sido bloqueada, sem mudanças; essa restrição foi resolvida pela autorização específica, não contornada.

Patch: somente `/etc/apache2/sites-available/roadrunners.run-le-ssl.conf` e `/etc/apache2/sites-available/www.roadrunners.run-le-ssl.conf`; `RemoteIPHeader CF-Connecting-IP`, 22 CIDRs oficiais Cloudflare, remoção tardia dos quatro headers de IP que o LocationResolver lê antes de CGI.REMOTE_ADDR. Mantém API, beta/dev, login, banco e `runner_dba` intactos; backup privado, configtest, reload gracioso e rollback dos dois arquivos em falha. Scripts/patches locais NÃO são recibos de execução.

Before SHA main `5b97dbe8d766881fade3c015c6f83458538ff492d923d91d0215e3b6abeffad7`; WWW `0903b5a939431d535ad73584e95fac3c0e6a37770c620099115075dcaaadd6a7`. Ambos0644/root:root. Patch separado `/private/tmp/rr-audience-activation.WBPcCt/remoteip-prod.patch`, SHA `02ee1ef6eaa15917720d245c3a7edcf8f62f22741877859711559f2c5e7d4197`. Fontes oficiais conferidas: Cloudflare ips-v4/ips-v6 e Apache mod_remoteip/mod_headers.

Retenção: servidor web sem Node, psql, usuário dedicado, unidades/cron de audiência ou serviço libpq nas localizações operacionais conhecidas. Não foram lidas senhas nem aberta conexão ao banco. [SQL não secreto para provisionamento pelo operador no DataGrip](2026-09-07_audiencia_retention_job_provision.sql) preparado, não executado; senha/configuração privada ainda necessárias. Não usar credenciais CF ou `runner_dba` como atalho.

Ativação: somente produção possui writer `APPLICATION.audienceMeasurement` nos settings remotos atuais; beta/dev não o possuem. `THIS.Name` é compartilhado. O override `config/audience.local.cfm` continua ausente. Instalar o arquivo sozinho não força recarga do mapa já completo. Avaliar a recarga controlada das configurações existentes sem alterar nome da aplicação ou invalidar sessões; ainda não executada. Não publicar endpoint administrativo novo para contornar controles.

Análise estática de 2026-09-08 pelo agente rr_reconcile_search, sem execução: o caminho já existente `/config/settings.cfm` canônico, sem query/cookies/Authorization, chega ao include de TargetPage e pode recarregar configurações sem stop/logout. Isso NÃO é recarga isolada de audiência: redefine também Ads/handoff/API/mobile/location/push, sem lock/rollback global. Arquivos privados de override não foram lidos. A viabilidade estática não equivale a homologação, e nenhuma requisição a esse caminho foi feita nesta continuação. Não usar `resetApp`.

Próxima dependência do operador: executar o SQL não secreto de provisionamento no DataGrip (`2026-09-07_audiencia_retention_job_provision.sql`) e retornar somente o SELECT final. O arquivo foi solicitado para abrir no painel do Codex. Não pedir senha pelo chat. Provisionar conexão privada/agenda depois; criação do papel sozinha não ativa a rotina. Nenhuma mudança SQL foi feita pelo agente.

Um check adicional do status/destino do POST no www, após a prova principal concluída, não executou por timeout de banner SSH. Não alterou estado. Não afirmar que esse redirecionamento adicional foi validado; os resultados www301 de robots e a prova de IP anteriores foram confirmados.

Prova de resultado: resposta200 do coletor com accepted>0 após navegação real e confirmação no Business. Um400 para POST vazio só prova validador ligado, não persistência. O painel tem cache de até um minuto; visitas admin/dev exigem filtro de internos para aparecer. DSN da audiência é explicitamente runnerhub→runner. Manter separados registros internos de validação e audiência comercial real. Coleta permanece desligada.
